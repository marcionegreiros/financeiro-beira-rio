# `components/` — UI compartilhada (placeholder)

Componentes reutilizáveis entre telas. Construídos a partir da **Fase 5**.

Destaques previstos:

- **`MedidorTanque`** — o **componente-assinatura** (§8.3): nível vertical de
  gasolina e diesel lado a lado, preenchimento proporcional, faixa de alerta,
  número grande de litros e % da capacidade. (Há uma prévia inline em
  [`../App.tsx`](../App.tsx).)
- Campos numéricos teclado-first com `tabular-nums` (classe `.numeros`).
- Cartões de KPI, tabelas de valores alinhados, badges de status.

> Antes de construir telas, carregar a skill `frontend-design` (§8). Seguir os
> tokens visuais de [`../index.css`](../index.css), não os defaults de "SaaS
> dashboard".
