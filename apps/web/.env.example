# Variáveis de ambiente do app (Vite carrega a partir de apps/web/).
# Copie para apps/web/.env e preencha na Fase 1 (provisionamento da nuvem).
# Apenas chaves PÚBLICAS (anon/publishable) — nada de service_role no cliente.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Endpoint da instância PowerSync (sync offline).
VITE_POWERSYNC_URL=
